# Demo authentication (frontend-only, no backend)
# Replace with non-sensitive placeholders or real values locally; never commit real secrets.
VITE_DEMO_EMAIL=demo@example.com
VITE_DEMO_PASSWORD=changeme
VITE_SESSION_SALT=replace-with-random-string
